{
  "name": "vscode-portfolio",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "next dev --hostname=0.0.0.0",
    "build": "NEXTEXPORT=1 next build && rm -r docs",
    "postbuild": "mv out docs && cp CNAME docs/CNAME && touch docs/.nojekyll"
  },
  "dependencies": {
    "next": "^15.2.3",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
